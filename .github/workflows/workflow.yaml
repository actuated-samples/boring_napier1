name: build

on: push
jobs:
  my-job:
    runs-on: actuated
    steps:
      - name: Docker run
        run: |
          git clone --depth=1 https://github.com/alexellis/inlets-connect
          cd inlets-connect
          docker build -t inlets-connect .
          docker images
